{
  "name": "v-scroller",
  "version": "1.0.0",
  "description": "原生 JavaScript 虚拟滚动条组件",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@rollup/pluginutils": "^5.0.0",
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  }
}
